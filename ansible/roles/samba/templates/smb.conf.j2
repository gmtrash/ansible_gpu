[global]
   workgroup = WORKGROUP
   server string = Forge Neo GPU Server
   security = user
   map to guest = Bad User
   dns proxy = no

   # Performance tuning
   socket options = TCP_NODELAY IPTOS_LOWDELAY SO_RCVBUF=131072 SO_SNDBUF=131072
   read raw = yes
   write raw = yes
   max xmit = 65535
   dead time = 15
   getwd cache = yes

   # Logging
   log file = /var/log/samba/log.%m
   max log size = 1000
   logging = file

[forge-models]
   comment = Stable Diffusion Models
   path = {{ forge_install_dir }}/app/models
   browseable = yes
   writable = yes
   guest ok = no
   valid users = {{ ansible_user }}
   create mask = 0644
   directory mask = 0755
   force user = {{ ansible_user }}
   force group = {{ ansible_user }}

[forge-outputs]
   comment = Stable Diffusion Generated Images
   path = {{ forge_install_dir }}/app/outputs
   browseable = yes
   writable = yes
   guest ok = no
   valid users = {{ ansible_user }}
   create mask = 0644
   directory mask = 0755
   force user = {{ ansible_user }}
   force group = {{ ansible_user }}
